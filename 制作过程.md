# 御前竞答 · 竞速答题网页应用 —— 制作过程汇报

## 一、项目目标
做一个可以在**现场大屏 + 观众手机**上一起玩的"竞速答题"互动小游戏：

- 主持人用电脑投大屏，展示题目、视频、倒计时、排名；
- 观众用手机**扫二维码**即可加入，起昵称、选头像、抢答；
- 答题前先播放"题目视频"，公布答案时播放"答案视频"；
- 整体采用"盛世天下 / 御前竞答"的宫廷风视觉；
- 一键启动，无需安装数据库或复杂环境。

## 二、技术选型
| 方面 | 方案 | 说明 |
| --- | --- | --- |
| 后端 | Node.js 原生 HTTP 服务 | 免框架，单文件 `server.js`，双击 bat 即启动 |
| 实时通信 | WebSocket | 主持人/手机与服务器实时同步答题状态、排名 |
| 前端 | 纯 HTML + 原生 JS | 三个页面，无需打包构建 |
| 视频 | 本地 `videos/` 文件夹 + Range 流式播放 | 支持边下边播、进度条拖动 |
| 头像 | 本地 `avatars/` 文件夹 | 手机端从头像库里选择 |
| 跨网络访问 | Cloudflare 快速隧道（cloudflared） | 手机不在同一 WiFi 也能扫码进入 |

## 三、系统结构
一个服务、三个页面：

- **`/host` 主持人大屏** —— 展示题目、视频、倒计时、实时作答人数、每题排名、最终金榜。
- **`/player` 手机端** —— 扫码加入、选头像、答题、看自己的得分与名次。
- **`/editor` 题目编辑器** —— 可视化增删题目、设正确答案、从下拉框选择题目视频/答案视频。
- 题库存放在 `data/questions.json`，编辑器保存后即时生效。

## 四、制作过程（里程碑）
1. **搭后端骨架**：Node.js 起 HTTP 服务，托管静态页面与 `videos/`、`avatars/` 静态资源，做了视频 Range（206）流式响应。
2. **接入 WebSocket 状态机**：设计比赛阶段 `lobby（大厅）→ intro（放题目视频）→ answering（答题倒计时）→ reveal（公布答案+排名）→ results（金榜）`，主持人操作驱动全场同步。
3. **手机端**：扫码进入、昵称+头像库选择、抢答与提交、答题倒计时、结果与名次展示。
4. **主持人大屏**：二维码入场、在场人数、题目/选项/视频、倒计时宫灯样式、每题排名、三甲领奖台与题目回顾。
5. **题目编辑器**：把视频改为"放进文件夹→下拉选择"的方式（无需上传），并加"刷新视频列表"。
6. **视觉风格**：宫廷红金配色、回纹装饰、楷体，主持大屏与手机端统一"御前竞答"主题。
7. **一键启动与跨网访问**：编写 `.bat` 启动脚本，并集成 Cloudflare 隧道生成公网二维码。

## 五、关键功能
- 扫码即玩，手机无需安装任何东西；
- 题目视频 / 每个选项的答案视频，均从本地文件夹选择；
- **竞速计分**：答对且越快得分越高（按剩余时间加权）；
- 实时排名、答对/答错/未作答标记、最终三甲领奖台与逐题回顾；
- 主持人可提前公布答案、踢人、重置返回大厅。

## 六、遇到的问题与解决
1. **手机扫码显示 Error 1033（Cloudflare 隧道错误）**
   - 原因：公司网络/防火墙拦截了 cloudflared 默认走的 QUIC/UDP，隧道地址生成了但连不上。
   - 解决：强制 `--protocol http2`（走 TCP 443）+ `--edge-ip-version 4`（仅 IPv4 边缘节点），并在启动日志里给出提示与"换用手机热点/局域网模式"的兜底方案。

2. **视频只有声音、没有画面**
   - 原因：素材全部是 HEVC/H.265 编码，浏览器只能解音轨、解不了画面。
   - 解决：内置绿色版 ffmpeg，把全部视频**转码为 H.264/AAC 的 MP4**（`+faststart` 适合网页播放）；题库改指向 mp4；编辑器自动隐藏同名旧 mov，只列可正常播放的 mp4；并附一个"转换视频.bat"供以后一键转码新素材。

3. **手机倒计时最后 5 秒，警告与普通倒计时同时显示、显得杂乱**
   - 解决：最后 5 秒隐藏普通"剩余 X 秒"，只显示红色闪烁警告"⚠️ 抓紧作答！仅剩 X 秒"。

4. **主持人点下一步时，上一步的视频还在播（甚至切到金榜后还有声音）**
   - 解决：进入答题阶段即停止题目视频；离开答题舞台（回大厅/看金榜）时停止残留视频，避免隐藏区仍有声音。

## 七、使用方式
1. 把题目视频放进 `videos/` 文件夹、头像图片放进 `avatars/` 文件夹；
2. 在 `/editor` 里编辑题目、设正确答案、选好每题的视频；
3. 双击启动脚本 → 电脑打开 `/host` 投大屏 → 观众扫码进入 `/player`；
4. 主持人点"开考"，按提示一步步推进即可。
